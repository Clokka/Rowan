{
  "name": "rowan",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "sass --watch static/scss:static/css",
    "build:css": "sass static/scss:static/css",
    "build": "rm -rf public && mkdir public && npm run build:css && cp templates/*.html public/ && cp -r static public/ && cp -r images public/ && cp -r videos public/ && cp favicon.ico public/ && cp CNAME public/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Clokka/Rowan.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Clokka/Rowan/issues"
  },
  "homepage": "https://github.com/Clokka/Rowan#readme",
  "description": "",
  "devDependencies": {
    "sass": "^1.89.2"
  }
}
